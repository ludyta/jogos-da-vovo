{
  "name": "jogos-da-vovo",
  "version": "1.0.0",
  "description": "App de jogos seguro para a vovó — sem anúncios, sem golpes",
  "main": "index.js",
  "scripts": {
    "build": "echo 'Nenhum build necessário - HTML puro'",
    "cap:add:android": "npx cap add android",
    "cap:sync": "npx cap sync",
    "cap:open": "npx cap open android",
    "setup": "npm install && npx cap add android && npx cap sync"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
